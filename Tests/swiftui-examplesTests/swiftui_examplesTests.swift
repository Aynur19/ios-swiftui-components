import XCTest
@testable import swiftui_examples

final class swiftui_examplesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftui_examples().text, "Hello, World!")
    }
}
